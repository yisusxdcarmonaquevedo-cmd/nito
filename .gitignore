# Entorno y dependencias
.venv/
__pycache__/
*.pyc

# Secretos — NUNCA subir (la clave va como Secret de GitHub Actions)
config/.env
.env

# data/ SÍ se versiona: la BD SQLite guarda el estado del pipeline
# (dedupe, cuota diaria de Gemini, caché de ratings) entre ejecuciones en la nube.

# Config local de Claude (estado de esta máquina)
.claude/settings.local.json

# Archivos temporales de wrangler (Cloudflare)
.wrangler/

# Tokens temporales (se borran tras usarlos)
cf_token.txt
gh_token.txt
